name: badua_coverage

on:
  push:
    branches: ["ISW2"]
  pull_request:
    branches: ["ISW2"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 1.8
        uses: actions/setup-java@v3
        with:
          java-version: 8
          distribution: 'temurin'

      - name: Install Ba-dua JAR files
        run: |
          mvn install:install-file -Dfile="badua/ba-dua-agent-rt-0.6.0-all.jar" -DgroupId="br.usp.each.saeg" -DartifactId=ba-dua-agent-rt -Dversion="0.6.0" -Dpackaging=jar
          mvn install:install-file -Dfile="badua/ba-dua-cli-0.6.0-all.jar" -DgroupId="br.usp.each.saeg" -DartifactId=ba-dua-cli -Dversion="0.6.0" -Dpackaging=jar
        working-directory: ${{ github.workspace }}

      - name: Build project with Ba-dua
        run: mvn clean verify -P badua
        working-directory: ${{ github.workspace }}/bookkeeper-server

      - name: Upload Ba-dua report
        uses: actions/upload-artifact@v3
        with:
          name: badua-report
          path: bookkeeper-server/target/badua.xml
          if-no-files-found: error
